# Core
numpy==1.26.4
opencv-contrib-python==4.10.0.84
pillow==10.4.0
tqdm==4.66.5
matplotlib==3.9.2
moviepy==1.0.3
imageio-ffmpeg>=0.4.9

# Optional (improves results)
torch>=2.1.0
torchvision>=0.16.0
clip-anytorch==2.6.0

# App
streamlit==1.38.0
requests>=2.32.3
setuptools<81  # quiet clip pkg_resources warning
easyocr>=1.7.1  # visual OCR (requires torch)
pyspellchecker>=0.8.1  # visual spelling
# Optional grammar checker (starts a local LanguageTool instance or uses remote API if configured)
language_tool_python>=2.8.1
openai-whisper>=20231117
google-cloud-aiplatform>=1.50.0
google-cloud-storage>=2.14.0
